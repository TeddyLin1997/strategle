type Tick = { price: string, open: string }
type TopItem = { name: string, symbol: string, icon: string  }
type WorldIndex = { name: string, symbol: string, country: string  }
type Commodity = { name: string, symbol: string, icon: string, description: string  }
